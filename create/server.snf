# https://dev.mysql.com/doc/refman/8.4/en/create-server.html

CREATE SERVER name
FOREIGN DATA WRAPPER wrapper OPTIONS (
    [ HOST 'host', ]
    [ DATABASE 'database', ]
    [ USER 'user', ]
    [ PASSWORD 'password', ]
    [ SOCKET 'socket', ]
    [ OWNER 'owner', ]
    [ PORT number, ]
)
