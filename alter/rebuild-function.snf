# https://dev.mysql.com/doc/refman/8.4/en/create-function.html

# CASE OPTIONS
CREATE [ DEFINER = user ] FUNCTION name (
    [ argument_definition [, ...] ]
)
    RETURNS type
    [ COMMENT 'comment' ]
    [ LANGUAGE SQL ]
    [ [ NOT ] DETERMINISTIC ]
    [ { CONTAINS SQL | NO SQL | READS SQL DATA | MODIFIES SQL DATA } ]
    [ SQL SECURITY { DEFINER | INVOKER } ]
    routine_statement

# WHERE argument_definition
argname argtype

# STATEMENT routine_statement
sql_statement
compound_statement
